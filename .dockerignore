# Dependencies (pnpm install handles this)
**/node_modules

# Client apps (not needed for server build)
apps/client
apps/map-editor

# Packages not needed by server
packages/components

# Build artifacts
**/dist
**/output

# IDE & Editor
.idea
.vscode

# Version control
.git
.gitignore

# Docker files（Dockerfile 本身由 docker 单独读取，这里只放行构建时需要的 entrypoint）
docker/*
!docker/entrypoint.sh

# Worktrees
.worktrees

# Misc
*.md
*.log
